<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <%= link_to student.name, student_path(student) %> - <%= student.house.name %>
  </div>
  <form action="" method="post">
  	<button>Delete</button>
  </form>
<% end %>